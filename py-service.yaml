    apiVersion: v1
    kind: Service
    metadata:
      name: py
    spec:
      selector:
        app: py-service
      ports:
        - protocol: TCP
          port: 8080
          targetPort: 8080
          nodePort: 30800 
      type: NodePort
